# --- Channel ---
# Which channel to use: discord (default)
CHANNEL_TYPE=discord

# Discord Bot Token
# https://discord.com/developers/applications
DISCORD_TOKEN=your_discord_bot_token_here

# --- AI ---
# Which AI provider to use: mock (default), copilot
AI_PROVIDER=mock

# GitHub (only needed when AI_PROVIDER=copilot)
# Token is auto-obtained via gh CLI or OAuth Device Flow, then saved here
GITHUB_TOKEN=
# OAuth Device Flow: register an OAuth App at https://github.com/settings/developers
# Only client_id is needed — no client_secret required
GITHUB_CLIENT_ID=
AI_MODEL=gpt-4o-mini

# Ollama (local AI — only needed when AI_PROVIDER=ollama)
OLLAMA_BASE_URL=http://localhost:11434

# --- Tools ---
# Brave Search (get API key at https://brave.com/search/api/)
BRAVE_API_KEY=

# Local file access (comma-separated allowed root paths, e.g. D:\,E:\docs)
LOCAL_FILES_ALLOWED_PATHS=

# --- Jobs ---
# Secrets only — operational config (schedule, notify_channel, etc.) lives in apps/bot/config/jobs.json
# Gmail monitoring: use a Gmail App Password https://myaccount.google.com/apppasswords
GMAIL_ADDRESS=
GMAIL_APP_PASSWORD=

# Outlook monitoring
OUTLOOK_ADDRESS=
OUTLOOK_APP_PASSWORD=

# QQ Mail monitoring: use an app password (授权码) from QQ Mail settings
# Settings → Account → POP3/IMAP/SMTP/Exchange → Generate Authorization Code
QQ_MAIL_ADDRESS=
QQ_MAIL_APP_PASSWORD=

# --- General ---
LOG_LEVEL=DEBUG
